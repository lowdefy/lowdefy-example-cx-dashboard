global:
  _ref: global.yaml

# menus:
#   - _ref: menu.yaml

connections:
  - id: cx_collection
    type: MongoDBCollection
    properties:
      databaseName: example-cx
      collection: cx-collection
      databaseUri:
        _secret: lowdefy_examples_mdb

pages:
  - _ref: projects/dashboard-main.yaml
  - _ref: projects/dashboard-data.yaml